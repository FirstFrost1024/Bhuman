# B-Human Code Release

This is the official 2025 B-Human code release. Documentation can be found [here](https://docs.b-human.de/coderelease2025/).

B-Human is a research project. We provide the software as is. We release it as a snapshot of our internal repository on a yearly basis. You can build on this software, but you have to make do with the documentation we released. You may report bugs or problems with newer releases of operating systems or the tools used by opening a GitHub issue. We might give hints on how to solve these problems if we also ran into them since the code release. Do not write emails to the team or any of its members directly. They will not be answered.

Before cloning this repository, you must read our [license terms](License.md) and the [instructions in our documentation](https://docs.b-human.de/coderelease2025/getting-started/initial-setup/).

Previous code releases are tagged with "coderelease&lt;year&gt;", where &lt;year&gt; is the year in which the code was released (starting with 2013).

<!-- BEGIN DEFAULT_CONFIG_DOC -->

## Default 配置总览与调参说明

> 范围：`Config/Robots/Default/*.cfg`（通用默认层）。`Config/Robots/Default/K1` 与 `Config/Robots/Default/T1` 为机型覆盖层，未覆盖项继承本节默认值。

> 调参建议：每次只改一组参数，先在仿真验证，再上机器人；优先记录步态稳定性、倒地率、踢球成功率、电流/温度、里程计漂移。

### `armContactModelProvider.cfg`
- 文件作用：手臂碰撞/接触检测参数
- 参数总数：8
- 参数解释与调参反馈：
  - `frameBufferSize`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `errorBufferSize`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `minimalContactDuration`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `yErrorThresholdBase`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `xErrorThresholdBase`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `yErrorThresholdExtension`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `xErrorThresholdExtension`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `soundWaitTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。

### `armKeyFrameEngine.cfg`
- 文件作用：手臂关键帧动作参数
- 参数总数：7
- 参数解释与调参反馈：
  - `allMotions`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `id`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `states`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `angles`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `stiffness`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `duration`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `interpolation`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。

### `audioProvider.cfg`
- 文件作用：音频采集参数
- 参数总数：8
- 参数解释与调参反馈：
  - `retries`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `retryDelay`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `allChannels`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `sampleRate`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `frames`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `onlySoundInSetAndPlaying`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `captureCard`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `captureVolume`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。

### `bodyContourProvider.cfg`
- 文件作用：身体轮廓参数（感知遮挡/自体轮廓）
- 参数总数：11
- 参数解释与调参反馈：
  - `torso`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `z`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `shoulder`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `upperArm`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `lowerArm`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `lowerArm2`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `upperLeg1`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `upperLeg2`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `foot`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。

### `boosterFallDownStateProvider.cfg`
- 文件作用：加速版跌倒检测参数（更激进的预测）
- 参数总数：27
- 参数解释与调参反馈：
  - `maxGyroToRegainStableState`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxVelForPrediction`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `useInertiaData`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `restrictiveFallDetection`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `playSounds`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `velocityDiscountFactor`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `forwardingTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minNormalizedFallVectorScalar`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minFallVectorScalar`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minPredictNormalizedFallVectorScalar`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minPredictFallVectorScalar`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minComDistanceToFootCenter`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minComDistanceToDetectFall`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxTorsoHeightToKeepSquatting`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minTorsoHeightToKeepUpright`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `sigmaArea`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `minTimeBetweenSound`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minTimeWithoutGroundContactToAssumePickup`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minTorsoOrientationToDetermineDirection`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxTimeStaggering`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `positionProcessDeviation`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `z`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `velocityProcessDeviation`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `positionMeasurementDeviation`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `velocityMeasurementDeviation`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。

### `cameraCalibration.cfg`
- 文件作用：相机外参修正参数
- 参数总数：7
- 参数解释与调参反馈：
  - `cameraRotationCorrections`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `upper`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `z`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `lower`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `bodyRotationCorrection`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。

### `cameraIntrinsics.cfg`
- 文件作用：相机内参参数（视场角、主点）
- 参数总数：8
- 参数解释与调参反馈：
  - `cameras`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `upper`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `openingAngleWidth`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `openingAngleHeight`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `opticalCenter`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `lower`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。

### `cameraResolution.cfg`
- 文件作用：相机分辨率配置参数
- 参数总数：4
- 参数解释与调参反馈：
  - `resolutions`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `upper`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `lower`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `timestamp`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。

### `coordinateSystemProvider.cfg`
- 文件作用：相机-机体坐标时间同步参数
- 参数总数：5
- 参数解释与调参反馈：
  - `cameraTimings`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `upper`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `imageRecordingTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `imageRecordingDelay`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `lower`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。

### `damageConfigurationBody.cfg`
- 文件作用：机身损伤仿真/屏蔽参数
- 参数总数：15
- 参数解释与调参反馈：
  - `noFieldGenuflect`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `brokenStandUp`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `brokenFsrSensors`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `left`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `right`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `jointsToEraseStiffness`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `jointsToMuteSirens`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `sides`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `weakLeg`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `footBumperDefect`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `armContactDefect`：接触/压力参数：调大更保守（不易判定接触变化）；调小更灵敏但可能抖动切换。
  - `startTiltLeft`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `startTiltRight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。

### `damageConfigurationHead.cfg`
- 文件作用：头部损伤仿真参数
- 参数总数：1
- 参数解释与调参反馈：
  - `audioChannelsDefect`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。

### `energySavingProvider.cfg`
- 文件作用：节能策略参数（温度/电流驱动降载）
- 参数总数：53
- 参数解释与调参反馈：
  - `motionChangeWaitTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `adjustWaitTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `currentThresholdLegs`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `currentThresholdLegsHighOffset`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `currentThresholdArms`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `minGearStep`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `maxGearStepArms`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `maxGearStepLegs`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `maxAngleMultipleJoints`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxAngleOneJoint`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxBaseOffsetOneJoint`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minNumberForHeatAdjustmentReset`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `resetTimeNormal`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `resetTimeSlow`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `stepsBeforeEmergencyStep`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `baseOffsetInterpolationTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `comXDiffRangeBeforeReset`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `min`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `max`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `comYDiffRangeBeforeReset`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `skipJoints`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `highStandSigns`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `headYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `headPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `waistYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lShoulderPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lShoulderRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lElbowYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lElbowRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lWristYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `rShoulderPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rShoulderRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rElbowYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rElbowRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rWristYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `lHipYawPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHipRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHipPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lKneePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lAnklePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lAnkleRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipYawPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rKneePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rAnklePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rAnkleRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `standSigns`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `specialJointHandling`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `allowBaseOffset`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `useGroundContactChange`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。

### `extendedFootSupportProvider.cfg`
- 文件作用：扩展足底支撑估计参数（预测+滤波）
- 参数总数：8
- 参数解释与调参反馈：
  - `outerWeight`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `innerWeight`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `predictionFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `discountFactorRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `min`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `max`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `currentRatioRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `currentSumPredictionFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。

### `fallDownStateProvider.cfg`
- 文件作用：跌倒状态估计阈值与预测参数
- 参数总数：26
- 参数解释与调参反馈：
  - `maxGyroToRegainStableState`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxVelForPrediction`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `useInertiaData`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `playSounds`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `velocityDiscountFactor`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `forwardingTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minNormalizedFallVectorScalar`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minFallVectorScalar`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minPredictNormalizedFallVectorScalar`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minPredictFallVectorScalar`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minComDistanceToFootCenter`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minComDistanceToDetectFall`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxTorsoHeightToKeepSquatting`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minTorsoHeightToKeepUpright`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `sigmaArea`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `minTimeBetweenSound`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minTimeWithoutGroundContactToAssumePickup`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minTorsoOrientationToDetermineDirection`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxTimeStaggering`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `positionProcessDeviation`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `z`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `velocityProcessDeviation`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `positionMeasurementDeviation`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `velocityMeasurementDeviation`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。

### `fallEngine.cfg`
- 文件作用：跌倒后恢复动作参数
- 参数总数：16
- 参数解释与调参反馈：
  - `waitAfterFalling`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `fallDirectionChangeThreshold`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `maxGyroToStartGetUp`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `lateFallYAngleBack`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lateFallYAngleFront`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `highStiffnessDuration`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `highStiffnessDurationElbowRoll`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `stiffnessArm`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `min`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `max`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `stiffnessLeg`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `stiffnessHead`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `shoulderPitchSideFallCorrectionRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `shoulderPitchSideFallCorrectionValue`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `shoulderRollBoostAngle`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `elbowYawBoostAngle`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。

### `filteredCurrentProvider.cfg`
- 文件作用：电机电流滤波与故障判定参数
- 参数总数：14
- 参数解释与调参反馈：
  - `stiffnessThreshold`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `stiffnessThresholdArms`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `flagsThreshold`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `checkWaitTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `motorMalfunctionTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `annotationWaitTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minJointDiffNormalJoints`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minJointDiffNormalJointsNoGroundContact`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minJointDiffAnkleRoll`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minJointDiffArms`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `ignoreJoints`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `specialMotorCheck`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `zeroCheckRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `filterSize`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。

### `footSupportProvider.cfg`
- 文件作用：基础足底支撑检测参数
- 参数总数：6
- 参数解释与调参反馈：
  - `outerWeight`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `innerWeight`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `currentSupportMaxPressure`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `currentSwingMinPressure`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `predictionFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `minSingleFSRPressureForPredictedSwitchFactor`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。

### `groundContactDetector.cfg`
- 文件作用：离地/触地检测参数
- 参数总数：3
- 参数解释与调参反馈：
  - `maxTimeWithoutPressure`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minTimeWithPressure`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minTimeSay`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。

### `headLimits.cfg`
- 文件作用：头部转动限制参数
- 参数总数：4
- 参数解释与调参反馈：
  - `shoulderRadius`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `intervals`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `lowerBounds`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `upperBounds`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。

### `imuCalibration.cfg`
- 文件作用：IMU标定参数（旋转、比例、零偏）
- 参数总数：11
- 参数解释与调参反馈：
  - `rotation`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `angle`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `axis`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `z`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `gyroFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `accFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `accOffset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `isCalibrated`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `serialNumberIMUCalibration`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。

### `imuCalibrationProvider.cfg`
- 文件作用：IMU自动标定策略参数
- 参数总数：7
- 参数解释与调参反馈：
  - `minStandStillTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `waitTimeTillNewCalibrationAccepted`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minTimeBetweenCalibration`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `isAutoCalibrationActive`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `maxtorsoAngles`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。

### `imuValueStateProvider.cfg`
- 文件作用：IMU质量与稳定性判定参数
- 参数总数：6
- 参数解释与调参反馈：
  - `thresholdGyroDeviation`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `thresholdZero`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `thresholdAccDeviation`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `maxMeanAccDeviation`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `bufferSize`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `accLengthBufferSize`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。

### `inertialDataProvider.cfg`
- 文件作用：惯导数据方差模型参数
- 参数总数：10
- 参数解释与调参反馈：
  - `gyroVariance`：噪声模型参数：调大更相信模型、弱化瞬时观测；调小更跟随传感器但对噪声更敏感。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `z`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `accVariance`：噪声模型参数：调大更相信模型、弱化瞬时观测；调小更跟随传感器但对噪声更敏感。
  - `accVarianceWhileWalking`：噪声模型参数：调大更相信模型、弱化瞬时观测；调小更跟随传感器但对噪声更敏感。
  - `accVarianceWhileStanding`：噪声模型参数：调大更相信模型、弱化瞬时观测；调小更跟随传感器但对噪声更敏感。
  - `accVarianceFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `fakeMeasurementVariance`：噪声模型参数：调大更相信模型、弱化瞬时观测；调小更跟随传感器但对噪声更敏感。
  - `copyRawData`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。

### `jointCalibration.cfg`
- 文件作用：关节零位校准参数
- 参数总数：28
- 参数解释与调参反馈：
  - `offsets`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `headYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `headPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `waistYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lShoulderPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lShoulderRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lElbowYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lElbowRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lWristYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `rShoulderPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rShoulderRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rElbowYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rElbowRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rWristYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `lHipYawPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHipRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHipPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lKneePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lAnklePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lAnkleRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipYawPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rKneePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rAnklePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rAnkleRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。

### `jointLimits.cfg`
- 文件作用：关节活动范围约束参数
- 参数总数：30
- 参数解释与调参反馈：
  - `limits`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `headYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `min`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `max`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `headPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `waistYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lShoulderPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lShoulderRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lElbowYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lElbowRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lWristYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `rShoulderPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rShoulderRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rElbowYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rElbowRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rWristYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `lHipYawPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHipRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHipPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lKneePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lAnklePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lAnkleRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipYawPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rKneePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rAnklePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rAnkleRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。

### `jointPlayProvider.cfg`
- 文件作用：关节间隙估计参数
- 参数总数：19
- 参数解释与调参反馈：
  - `lowpassFilterFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `min`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `max`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `interpolateLowpassFilterTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `interpolateJointPlayValueWalkTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minWalkTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `jointPlayList`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `joint`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `ref`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `ratio`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `jointPlayScalingWalkingSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `maxForwardSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `minForwardSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `jointPlayScaling`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `warningSoundWaitTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minPlayForSound`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minPlaySlowForSound`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `numOfFramesSkip`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `motionDelay`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。

### `keyframeMotionEngine.cfg`
- 文件作用：关键帧动作编排（起身、倒地、特殊动作）
- 参数总数：135
- 参数解释与调参反馈：
  - `maxTryCounter`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `motorMalfunctionBreakUp`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxStiffnessDebugMode`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `motions`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `front`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `keyframes`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `balanceOut`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `keyframeEndRequest`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `energySavingLegs`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `energySavingArms`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `isStandHigh`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `isMotionStable`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `startAsNextPhaseMotion`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `back`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `recoverGeneric`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `startAsNextPhase`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `conditions`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `variable`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `range`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `min`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `max`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `isNot`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `isAndCondition`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `catchBack`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `catchFront`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `recoverFromGenu`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `recoverFromSumo`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `restartFront`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `restartBack`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `stand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `sit`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `sitDown`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `sitDownKeeper`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `keeperJumpLeft`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `genuflectStand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `genuflectStandDefender`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `demoBannerWave`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `demoBannerWaveInitial`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `keyframeBlock`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `baseLimbStiffness`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `phase`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `duration`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `goalCom`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `setLastCom`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `forbidWaitBreak`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `interpolationType`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `jointCompensation`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `waitConditions`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `keyframeBranches`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `balanceWithJoints`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `jointY`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `joint`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `scaling`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `jointBalanceType`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `jointX`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `singleMotorStiffnessChange`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `angles`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `head`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `positions`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `leftArm`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `rightArm`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `leftLeg`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `rightLeg`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `robotPoses`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `torsoAngleBreakUpEnd`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `pitchDirection`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rollDirection`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `torsoAngleBreakUpStart`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `s`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `blockID`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `motionID`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `initStiffness`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `removeCurrentBlock`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `preCondition`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `isAnd`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `useEarlyEntrance`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `earlyEntranceCondition`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxNumberOfLoopsBlockID`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxNumberOfLoopsMotionID`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `standHigh`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `jointDelta`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `hipPitchDifferenceCompensation`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `jointPairs`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `predictJointDiff`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxWaitTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `frontCatch`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `frontCatchStart`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `frontUpright`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `backTipOver`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `backTipOverReset`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `recover`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `recoverBackStaticArms`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `recoverFront`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `recoverBack`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `recoverSlow`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `recoverGenuflect`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `recoverGenuflectWide`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `armSavingFront`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `armSavingBack`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `fromSitting`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `genuflectFromSitting`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `genuflectFromStand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `genuflect`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `genuflectWide`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `genuflectEnd`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `wait`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `jumpLeftFromSitting`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `jumpLeftFromStand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `sitKeeper`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `sitKeeperJump`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `waveInitial`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `wave`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `balanceOutParams`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minFluctuation`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minForwardAngle`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minBackwardAngle`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minPIDDValue`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `supportPolygonOffsets`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `z`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `safeFallParameters`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `sitHYPThreshold`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `headStiffness`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `bodyStiffness`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `hipPitchStiffness`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `elbowRollStiffness`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `lowHeadStiffnessWaitTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `unstiffWaitTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `safeUprightParameters`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `jointDiffFilterParameters`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `oscillationPeriod`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `oscillationAmplitude`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `hypErrorOscillationScaling`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。

### `keyframeMotionParameters.cfg`
- 文件作用：关键帧平衡/增益参数（PID与附加缩放）
- 参数总数：23
- 参数解释与调参反馈：
  - `balanceList`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `UnknownPhase`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `balanceFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `maxValAdditionalScaling`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `pitchDirection`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `min`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `max`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `rollDirection`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `exponentFactorScaling`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `pidPPitchParameter`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `baseValue`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxAdditionalValue`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `pidPRollParameter`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `pidDPitchParameter`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `pidDRollParameter`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `comDiffBase`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `PushingWithArmsFromGround`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `ReduceVel`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `Split`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `HalfSplit`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `Sit`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `Stand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `StandStatic`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。

### `massCalibration.cfg`
- 文件作用：质量与惯量参数
- 参数总数：33
- 参数解释与调参反馈：
  - `masses`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `neck`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `mass`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `offset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `z`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `inertia`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `head`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `shoulderLeft`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `bicepsLeft`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `elbowLeft`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `foreArmLeft`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `wristLeft`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `shoulderRight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `bicepsRight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `elbowRight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `foreArmRight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `wristRight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `pelvisLeft`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `hipLeft`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `thighLeft`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `tibiaLeft`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `ankleLeft`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `footLeft`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `pelvisRight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `hipRight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `thighRight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `tibiaRight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `ankleRight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `footRight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `torso`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `waist`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。

### `modelPoints.cfg`
- 文件作用：模型采样点参数（腿/脚碰撞与几何近似）
- 参数总数：6
- 参数解释与调参反馈：
  - `thighPoints`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `thighIndex`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `shinPoints`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `shinIndex`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `footPoints`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `footIndex`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。

### `modifiedJointRequestProvider.cfg`
- 文件作用：关节请求修正参数（抑制超调/震荡）
- 参数总数：36
- 参数解释与调参反馈：
  - `iFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `headYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `stand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `min`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `max`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `walk`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `removeFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `maxControlValue`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `headPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `waistYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lShoulderPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lShoulderRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lElbowYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lElbowRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lWristYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `rShoulderPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rShoulderRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rElbowYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rElbowRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rWristYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `lHipYawPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHipRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHipPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lKneePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lAnklePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lAnkleRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipYawPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rKneePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rAnklePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rAnkleRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `removeIValueSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `removeDuration`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。

### `motionEngine.cfg`
- 文件作用：运动总调度参数（动作切换、故障降级）
- 参数总数：8
- 参数解释与调参反馈：
  - `emergencySitDownDelay`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `brokenJointAutomaticStiffness`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `brokenJointsReducesStiffnessList`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `uprightAngle`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `uprightWarningTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `forcePlayDead`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。

### `odometryDataPreviewProvider.cfg`
- 文件作用：步态里程计预估参数
- 参数总数：6
- 参数解释与调参反馈：
  - `useMeasuredSwingAfterStepStartedTime`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `usePlannedIsLeftPhaseAfterThisSwitchTime`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `odometryWalkScaling`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `filterSupportSwitch`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。

### `replayWalkRequestProvider.cfg`
- 文件作用：回放步态请求参数
- 参数总数：67
- 参数解释与调参反馈：
  - `motionRequests`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `description`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `isLeftPhaseStart`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `walkRequests`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `walkKickStep`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `keyframe`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `longKickParams`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `increaseSwingHeightFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `reduceSwingFootHeight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `numOfBalanceSteps`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `useSlowSupportFootHeightAfterKickInterpolation`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `useSlowSwingFootHeightInterpolation`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `overrideOldSwingFoot`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `overrideOldSupportFoot`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `currentKick`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `useLastKeyframeForSupportFootXTranslation`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `usedWalkDelay`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `currentKickVariant`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `lastStepInfo`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `nextIsLeftPhase`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `wasLastPhaseLeftPhase`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `wasLastPhaseInWalkKick`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `leftStartOffset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rotation`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `translation`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `rightStartOffset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lastExecutedStep`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `isReplayWalkRequest`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `isWalkKickPhase`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `step`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `delay`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `stepTarget`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `stepTargetSwing`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `stepTargetConverted`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `stepTargetSwingConverted`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `stepRatio`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `speedUpSwing`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `holdXSwingTarget`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `holdXSupportTarget`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `reachedWaitPosition`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `interpolationType`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `joint`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `offset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `minRatio`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `middleRatio`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `maxRatio`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minimumRatio`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `shiftByKeyFrame`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `kickType`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `walkKickType`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `kickLeg`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `precision`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `length`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `power`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `direction`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `kickInterpolation`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `shiftTurnKickPose`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `ignoreNoStandPhaseRule`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `diagonalKickInfo`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `diagonalKickState`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `contactPoint`：接触/压力参数：调大更保守（不易判定接触变化）；调小更灵敏但可能抖动切换。
  - `ballEstimationTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `delayParams`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `kickIndex`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `forceDelay`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。

### `restrictiveFallEngine.cfg`
- 文件作用：保守起身策略参数（更重安全约束）
- 参数总数：11
- 参数解释与调参反馈：
  - `waitAfterFalling`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `transitionDelay`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `soundTimeDelay`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `maxGyroToStartGetUp`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxPositionDifference`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `jointSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `uprightThreshold`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `fallAngle`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `stiffness`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `standHeight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `allowGetUp`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。

### `rlWalkingEngine.cfg`
- 文件作用：RL步态引擎参数（策略速度、转向/平移耦合、步态周期）
- 参数总数：46
- 参数解释与调参反馈：
  - `configuredParameters`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `rotation`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `translation`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxSpeedBackwards`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `thresholdStopStandTransition`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `baseWalkPeriod`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `stepSizeParameters`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `insideTurnRatio`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `reduceTranslationFromRotation`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `noTranslationFromRotation`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `noTranslationYFromRotationFastInner`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `noTranslationYFromRotationFastOuter`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `reduceTranslationYFromRotationFast`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `minXTranslationStep`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `minXForwardTranslationFast`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minXBackwardTranslationFastRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `min`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `max`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `translationPolygonSafeRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `noFastTranslationPolygonStepsNumber`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `commonSpeedParameters`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `maxAcceleration`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxDeceleration`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `armParameters`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `armShoulderRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `armShoulderRollIncreaseFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `armShoulderPitchFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `armShoulderPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `armElbowYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `walkNeuronalNetworkParameters`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `modelName`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `velocityFactor`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `actionClipRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `useWaist`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `numInput`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `numOutput`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `timeLowWalkSpeedForStand`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `slowWalkStepSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `ballNetworkParameters`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `active`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `frequencyParameters`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `base`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `clipRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。

### `robotDimensions.cfg`
- 文件作用：机器人几何参数（连杆长度、偏置）
- 参数总数：40
- 参数解释与调参反馈：
  - `yHipOffset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `hipPitchToRollOffset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `z`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `upperLegLength`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `xOffsetHipToKnee`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lowerLegLength`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `zOffsetAnklePitchToRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `footHeight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `footLength`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `soleToFrontEdgeLength`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `soleToInnerEdgeLength`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `soleToOuterEdgeLength`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `soleToBackEdgeLength`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `soleShape`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `bumperInnerEdge`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `bumperOuterEdge`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `hipToNeckOffset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `offsetNeckToLowerCamera`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `tiltNeckToLowerCamera`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `offsetNeckToUpperCamera`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `tiltNeckToUpperCamera`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `armOffset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `yOffsetElbowToShoulder`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `upperArmLength`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `lowerArmLength`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `xOffsetElbowToWrist`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `handOffset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `handRadius`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `armRadius`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `imuOffset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `leftFsrPositions`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `fl`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `fr`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `bl`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `br`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `rightFsrPositions`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `simOriginHeight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `robotDepth`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。

### `robotHealthProvider.cfg`
- 文件作用：健康状态播报阈值（电量/温度）
- 参数总数：4
- 参数解释与调参反馈：
  - `batteryLow`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `cpuHeat`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `timeBetweenHeatScreams`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `enableName`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。

### `robotStableStateProvider.cfg`
- 文件作用：机体稳定状态预测参数
- 参数总数：3
- 参数解释与调参反馈：
  - `predictionSteps`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `turnPointSteps`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `useLightModel`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。

### `solePressureStateProvider.cfg`
- 文件作用：足底压力状态与切换检测参数
- 参数总数：22
- 参数解释与调参反馈：
  - `minPressure`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `min`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `max`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `badMinPressureTimewindow`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minPressurePercent`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxPressure`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minPressureRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minPressureInterpolationValues`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `numOfSupportSwitches`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxTimeBetweenSupportSwitches`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minTimeBetweenSupportSwitches`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `maxTimeLegNoPressureForCalibration`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `highestPressureUpdateTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `lowestPressure`：接触/压力参数：调大更保守（不易判定接触变化）；调小更灵敏但可能抖动切换。
  - `left`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `fl`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `fr`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `bl`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `br`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `right`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `isCalibrated`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `allowRecalibration`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。

### `staticJointPoses.cfg`
- 文件作用：静态姿态库（坐下/站立等）
- 参数总数：34
- 参数解释与调参反馈：
  - `pose`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `sit`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `headYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `headPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `waistYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lShoulderPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lShoulderRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lElbowYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lElbowRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lWristYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `rShoulderPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rShoulderRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rElbowYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rElbowRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rWristYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `lHipYawPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHipRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHipPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lKneePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lAnklePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lAnkleRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipYawPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rKneePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rAnklePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rAnkleRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `sitBack`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `sitFront`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `sitFrontGetUp`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `sitBackAfterFall`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `sitFrontAfterFall`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。

### `stiffnessSettings.cfg`
- 文件作用：关节刚度默认配置
- 参数总数：28
- 参数解释与调参反馈：
  - `stiffnesses`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `headYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `headPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `waistYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lShoulderPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lShoulderRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lElbowYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lElbowRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lWristYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `rShoulderPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rShoulderRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rElbowYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rElbowRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rWristYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHand`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `lHipYawPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHipRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lHipPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lKneePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lAnklePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `lAnkleRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipYawPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rHipPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rKneePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rAnklePitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `rAnkleRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。

### `walkKickEngine.cfg`
- 文件作用：行走踢球参数（踢球轨迹、触球时机、落脚稳定）
- 参数总数：100
- 参数解释与调参反馈：
  - `playKickSounds`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `allowDiagonalKicks`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `allowJustHitTheBall`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `allowDelay`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `maxTurnLeftFoot`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `min`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `max`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxForward`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxSide`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `rotation`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `translation`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxForwardAcceleration`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `walkKicksWithRestrictedStart`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `stabilizationWalkDelay`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `restrictedYTranslationOfPreStepWithStabilization`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `restrictedYTranslationOfPreStep`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `sidewardsOuterForwardRestriction`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `forwardLongFirstStepAnkleCompensationRange`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `forwardLongFirstStepAnkleCompensationAngle`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `forwardLongMaxStepSize`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `forwardLongMaxStepSizeDemo`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `justHitTheBallMaxStepSize`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `forwardTurnPreciseMaxStepSize`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `turnKickPreviousMaxXStepSize`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `forwardStealVFeetAngle`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `forwardStealWaitingKickPose`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `forwardStealVFeetAngleFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `forwardPreStepSkipMaxKickAngle`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `forwardStealMinXAbortThreshold`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `timeSinceBallLastSeenThreshold`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `maxClipBeforeAbortForwardSteal`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `forwardStealMaxPreviousYTranslation`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `stepDuration`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `timeToHitBall`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `walkKicksList`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `none`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxXDeviation`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxYDeviation`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `additionalXDeviation`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `additionalYDeviation`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `additionalXDeviationJustHitTheBall`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `additionalYDeviationJustHitTheBall`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxAngleDeviation`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxClipBeforeAbortX`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxClipBeforeAbortY`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxClipBeforeAbortRot`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxClipBeforeAbortJustHitTheBallX`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxClipBeforeAbortJustHitTheBallY`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxClipBeforeAbortJustHitTheBallRot`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `sideDeviationScalingBasedOnLastStep`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `kickKeyFrame`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `forward`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `reduceSwingFootHeight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `increaseSwingHeightFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `maxSideStep`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `useSlowSupportFootHeightAfterKickInterpolation`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `isAfterKick`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `clipMaxSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `useSlowSwingFootHeightInterpolation`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `overrideOldSwingFoot`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `overrideOldSupportFoot`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `numOfBalanceSteps`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `useLastKeyframeForSupportFootXTranslation`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `ignoreClipAbortCheck`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `keyframes`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `relativePositionToBallMax`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `relativePositionToBallMin`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `offsetSwingFootMax`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `offsetSwingFootMin`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minStepTarget`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `relativeBackwardStep`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `directionRatio`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `reachPositionRatio`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `speedUpSwingPositionXScaling`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `speedUpSwingPositionYScaling`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `useDefaultReachPositionRatio`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `holdXTranslationWhenMovingBackward`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `holdYTranslationWhenFeetTogether`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `holdXSwingTarget`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `holdXSupportTarget`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `returnXToZero`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `returnYToZero`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `returnRotationToZero`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `hold`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `jointOffsets`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `joint`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `offset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `minRatio`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `middleRatio`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `maxRatio`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minimumRatio`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `shiftByKeyFrame`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `forwardLong`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `sidewardsOuter`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `turnOut`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `forwardSteal`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `forwardAlternative`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。

### `walkLearnerProvider.cfg`
- 文件作用：步态学习辅助参数
- 参数总数：11
- 参数解释与调参反馈：
  - `adjustmentRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `min`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `max`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `adjustmentSteps`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `minForwardStep`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `maxSideStep`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `maxTurnStep`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `clipStepDurationRatio`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `maxStepsForJointPlayInitializing`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `jointPlayInitializing`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `bestStepDuration`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。

### `walkingEngine.cfg`
- 文件作用：传统步态引擎参数（速度包络、停走过渡、步态边界）
- 参数总数：28
- 参数解释与调参反馈：
  - `configuredParameters`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `rotation`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `translation`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `minSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `maxSideWithMaxForwardSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `maxSpeedBackwards`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `minSpeedBackwards`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `emergencyNotMovingTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `thresholdStopStandTransition`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `thresholdDiveStandTransition`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `supportSwitchPhaseRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `min`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `max`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `jointPlayOffsetParameters`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `applyOffsetFactorHipPitch`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `applyOffsetFactorAnkle`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `maxFactorBase`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `applyOffsetFactorGeneral`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `walkSpeedParams`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `dynamicStepUpdate`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `transitionWalkToKick`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `dynamicKickPoseUpdate`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `dynamicKickDelayUpdate`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `energySavingWalk`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `dynamicKickStepUpdate`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。

### `walkingEngineCommon.cfg`
- 文件作用：步态公共参数（相位切换、摆腿/支撑腿、稳定性、避碰）
- 参数总数：200
- 参数解释与调参反馈：
  - `minPhaseForStopWithWrongGroundContact`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `clipAtBallDistanceX`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `clipAtBallDistance`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `standHighTorsoPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `standHighInterpolationDuration`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `standInterpolationVelocity`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `maxStandInterpolationDuration`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `standHighNotMovingTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minTimeForEarlySwitch`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `maxWalkDirectionForFootPrediction`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `useFootSupportSwitchPrediction`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `blockStoppingWithStepAdjustment`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `useJointPlayScaling`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `commonSpeedParameters`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `maxAcceleration`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `x`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `y`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxDeceleration`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `fastFeetAdjustment`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `slowFeetAdjustment`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `soleRotationOffsetSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `min`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `max`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `soleRotationOffsetSpeedAfterKickTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `walkSpeedReductionFactor`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `reduceWalkingSpeedStepAdjustmentSteps`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `afterKickFeetHeightAdjustment`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxSwingStretchLength`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxSwingSideStretch`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `swingStretchAfterOvertime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `kinematicParameters`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `baseWalkPeriod`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `sidewaysWalkHeightPeriodIncreaseFactor`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `sidewaysHipShiftFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `walkHipHeight`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `energyEfficientWalkSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `maxSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `rotation`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `translation`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxSpeedBackwards`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `noEfficientWalkSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `hipHeightChangeSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `baseFootLift`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `torsoOffset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `legLengthClipThreshold`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `yHipOffset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `emergencyStep`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `emergencyStepSize`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `emergencyStepHeightFactor`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `emergencyMaxGyroMean`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `emergencyMaxGyroDeviation`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `emergencyMaxZGyroDeviation`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `emergencyAfterStepDuration`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `armParameters`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `armShoulderRoll`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `armShoulderRollIncreaseFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `armShoulderPitchFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `comTiltFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `armInterpolationTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `armShoulderPitch`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `armElbowYaw`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `stepSizeParameters`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `insideTurnRatio`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `reduceTranslationFromRotation`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `noTranslationFromRotation`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `noTranslationYFromRotationFastInner`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `noTranslationYFromRotationFastOuter`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `reduceTranslationYFromRotationFast`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `minXTranslationStep`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `minXForwardTranslationFast`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minXBackwardTranslationFastRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `translationPolygonSafeRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `noFastTranslationPolygonStepsNumber`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `balanceParameters`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `gyroLowPassRatio`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `gyroForwardBalanceFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `gyroBackwardBalanceFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `gyroSidewaysBalanceFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `gyroHipKneeBalanceFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `gyroHipKneeKickBalanceFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `slowdownTorsoOffset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `slowdownFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `minTorsoRotation`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `allowSlowDownWhenWalkingBackwards`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `sideTorsoRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `sideComRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `gyroSideTorsoBalance`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxGyroAnkleBalance`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `walkStepAdjustmentParams`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `maxVelX`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minVelX`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `removeSpeedX`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `comLowPassRatio`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `desiredFootAreaSmall`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `desiredFootAreaBig`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `desiredFootAreaForwardOvertime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `overtimeRange`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `footAreaWalkSpeedScale`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `unstableWalkThreshold`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `reduceWalkingSpeedTimeWindow`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `useFullSwingSole`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `swingAcc`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `supportAcc`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `useAccelerations`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `soleRotationParameter`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `maxTorsoRotation`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `soleCompensationBackwardReduction`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `soleCompensationSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `maxRollAdjustment`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `measuredErrorFactor`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `reductionTimeFactor`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `measuredErrorTimeScaling`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `timeScaling`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `timeScalingRoll`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `sideSizeXRotationScaling`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `removeYCompensationAtStart`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `tiltErrorDiffOffset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `tiltErrorDiffScaling`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `gyroScaling`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `torsoRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `deltaRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `comXRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxStepRatioToStart`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `minGyro`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minSideStepSize`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `deltaRangeSecondStep`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `scalingClipRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minMaxSumScaling`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `parabolicFootHeightParameters`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxHeightAfterTime`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `maxHeightAfterTimePercent`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `walkStiffnessParameters`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `walkStiffness`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `pickedUpStiffness`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `lowStiffnessDelay`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `standStiffnessDelay`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `lowStiffnessLegs`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `lowStiffnessAnklePitch`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `lowWalkStiffness`：刚度类参数：调大姿态更“硬”且抗扰更强，但电流和温升更高；调小更省电、柔顺，但易塌腰/打滑。
  - `speedControlParams`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `rotationSpeedHYP`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `rotationSpeedHip`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `rotationSpeedAnklePitch`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `rotationSpeedKnee`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `rotationSpeedRoll`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `pitchRatioForward`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `pitchRatioBackward`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `rollRatio`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `rotationErrorRatioForwardWornOut`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `rotationErrorRatioForwardGood`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `rotationErrorRatioBackward`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `walkDelayParameters`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minDelay`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `heightOffset`：几何姿态参数：用于标定与姿态修正；偏大/偏小会引入系统性方向误差（如走偏、感知偏移）。
  - `endHeightShift`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `delayInterpolation`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `sideShift`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `sideShiftDelayInterpolation`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `translationBuffer`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `kickTimeOffset`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `kickTimeBallDistanceScaling`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `walkSpeedParamsWalkStep`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `sideStabilizeParameters`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `turnIncreaseRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `increaseThreshold`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `minOuterSide`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `minOuterSideStop`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `sideHipShiftStepSizeRange`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `maxSideHipShift`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `maxSideHipShiftStepSize`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `comInOuterInterpolationRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `heightRange`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `firstStepHeightRange`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `lastLastWalkPhaseStepDuration`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `lastWalkPhaseStepDuration`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `delayRange`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `minHipShiftRatioForDelay`：时间类参数：调大通常更平滑/更稳但响应变慢；调小响应更快但更容易误触发。
  - `maxNextSideStepSize`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `walkHipShiftPhaseParameters`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `shiftSpeed`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `shiftSpeedStart`：速度/周期类参数：调大动作更快、覆盖更强但稳定性压力更高；调小更稳但机动性下降。
  - `comYStopConditionWalking`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `comYStopConditionStarting`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `maxShift`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `comShiftScalingBackward`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `comShiftScalingForward`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `comShiftScalingWalkingBackward`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `comShiftScalingWalkingForward`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `deltaScaling`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `minShiftStartWalking`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `useHipShift`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `sideCatchWalkParameters`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `allowedAdjustmentDuringTimeRatio`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `comYThreshold`：阈值类参数：调大更不敏感、误报更少但漏检风险更高；调小更敏感、响应更快但更容易抖动。
  - `maxAdjustment`：范围约束参数：放宽可提升动作空间与速度上限，但越界和不稳定风险上升；收紧更安全。
  - `supportCompensationParameters`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。
  - `lowPassFilterSlow`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `lowPassFilterFast`：增益/权重类参数：调大该分支影响更强、收敛更快但易过冲；调小更平滑但效果减弱。
  - `isActive`：开关类参数：开启提高相关功能覆盖率，但可能增加资源消耗或触发更激进行为；关闭更保守。
  - `scaling`：通用行为参数：建议单次小步调整（5%-10%），观察稳定性、速度、能耗和故障率四个指标。


<!-- END DEFAULT_CONFIG_DOC -->
